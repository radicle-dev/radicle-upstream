// Shared types and functionality related to styling

export type ButtonVariant =
  | "vanilla"
  | "primary"
  | "secondary"
  | "transparent"
  | "outline"
  | "destructive"
  | "embedded";

export enum CSSPosition {
  Top = "top",
  Right = "right",
  Bottom = "bottom",
  Left = "left",
}
